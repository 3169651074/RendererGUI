//页面元素的默认值，在渲染js中动态设置此处定义的值
const defaultTitleColor = "#FFFFFF"

const defaultResolution = {
    width: 1920, height: 1080
};

const defaultBackgroundColor = {
    r: 179,
    g: 204,
    b: 229,
    hex: "#B3CCE5"
};

const defaultCameraUp = "y";
const defaultFOV = 90;
const defaultSPP = 100;
const defaultMaxDepth = 50;
